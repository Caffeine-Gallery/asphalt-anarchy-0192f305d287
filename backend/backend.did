service : {
  addScore: (text, nat) -> (vec record {
                                  text;
                                  nat;
                                });
  getHighScores: () -> (vec record {
                              text;
                              nat;
                            }) query;
}
